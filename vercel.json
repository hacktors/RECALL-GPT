{
  "installCommand": "if [ -d frontend ]; then npm --prefix frontend ci; else npm ci; fi",
  "buildCommand": "if [ -d frontend ]; then npm --prefix frontend run build && rm -rf dist && cp -R frontend/dist dist; else npm run build; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://recall-gpt.onrender.com/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
